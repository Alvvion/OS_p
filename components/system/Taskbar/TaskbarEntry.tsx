import { StyledTaskbarEntry } from "@/styles/components/system/StyledTaskbar";

const TaskbarEntry: React.FC = () => (
  <StyledTaskbarEntry>Entry</StyledTaskbarEntry>
);

export default TaskbarEntry;
